import QtQuick
import QtQuick.Controls

Item {

    height: 30
    width: parent.width

    // Rectangle to house the other header elements
    Rectangle
    {
        id: headerBox
        height: 30
        width: parent.width

        color: "Gray"

    }
}
